import injector from 'vue-inject';

function {{pascalCase name}}(apiUrlBuilder, httpHandlerService, userService, moment, serviceEndpoint) {
    return {

    }
}

injector.service('{{pascalCase name}}', ['moment'], {{pascalCase name}});